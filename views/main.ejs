<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plight-weather</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet">
</head>
<body>

<body background="videos\background3.jpg">
    <div class="main-container">
        <div class="navigation">
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
          <p class="navbar-brand" href="#">My Weather</p>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Home</a>
              </li>
              <li class="nav-item">
                <p class="nav-link" ><%= cityname %></p>
              </li>
              <!-- <li class="nav-item">
                <p class="nav-link" ><%= cityname %></p>
              </li> -->
            </ul>
            <form class="d-flex" role="search" action="/" method="post">
              <input class="form-control me-2" type="text"  name="cityName" id="">
              <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
          </div>
        </div>
    </nav>
</div>
<% if (temperature) { %>
<div class="grid-container">
  
  <div class="left-panel">
      <!-- Content for the left panel -->
      <h2><%=temperature%>&deg;C</h2>
      <h1><%= cityname %></h1>

      <p><%= weatherCondition %><img src="<%= ImageURL %>"></p>
  </div>
  <div class="right-panel">
      <!-- Content for the right panel -->
      <h2>Weather Details</h2>
      <div class="pre-text">
      <pre>Feels Like                <%= feelsLike %>&deg;C</pre> 
      <pre>Humidity                 <%= humidity %>%</pre>
      <pre>Wind                        <%= windSpeed %>KM/Hr</pre>
      <pre>Air Pressure           <%= airPressure %>hPa</pre>
      <pre>Sunrise/Sunset      <%=Sunrise%>/<%=Sunset%></pre>
      </div>
  </div>
</div>
<% } else { %>
  <!-- Display a message when weather data is not available -->
  <p>No weather data available for the city <%= cityname %></p>
  <p><%=error%></p>
<% } %>
    </div>
</body>
</html>
